---
phase: quick
plan: 260315-on9
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/marcu/AppData/Roaming/Code/User/settings.json
autonomous: false
requirements: [QUICK-on9]

must_haves:
  truths:
    - "VS Code editor font is Calibri on Windows"
    - "VS Code editor font falls back gracefully on Mac (Calibri is system font on macOS 15+; earlier macOS uses fallback)"
  artifacts:
    - path: "C:/Users/marcu/AppData/Roaming/Code/User/settings.json"
      provides: "editor.fontFamily with Calibri as primary, cross-platform fallbacks"
      contains: "editor.fontFamily"
  key_links: []
---

<objective>
Set the VS Code editor font to Calibri for both Prism theme variants.

Purpose: Calibri is preferred as the editor font across Windows and Mac. The setting is global (user-level settings.json) so it applies regardless of which color theme (Prism light or dark) is active.
Output: Updated VS Code settings.json with editor.fontFamily set to Calibri with cross-platform fallbacks.
</objective>

<execution_context>
@C:/Users/marcu/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/marcu/AppData/Roaming/Code/User/settings.json

The current settings.json has no `editor.fontFamily` entry. Calibri is a Microsoft font bundled with Windows and available on macOS when Office is installed. It is also included natively in macOS 15 (Sequoia). For older macOS without Calibri, a sans-serif fallback is needed.

Font family string to use: `"Calibri, 'Helvetica Neue', Arial, sans-serif"`
- Windows: Calibri renders natively
- Mac (Sequoia / Office installed): Calibri renders natively
- Mac (older, no Office): Falls back to Helvetica Neue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editor.fontFamily to VS Code settings</name>
  <files>C:/Users/marcu/AppData/Roaming/Code/User/settings.json</files>
  <action>
    Read the current settings.json (shown in context above). Add two new entries inside the existing JSON object:

    ```json
    "editor.fontFamily": "Calibri, 'Helvetica Neue', Arial, sans-serif",
    "editor.fontSize": 13
    ```

    Do NOT change any existing keys. Do NOT remove `workbench.colorTheme` or any other key. The fontSize of 13 is appropriate for Calibri which renders slightly smaller than monospace fonts at the same nominal size — leave at 13 unless a different size was specified.

    The file is at: C:/Users/marcu/AppData/Roaming/Code/User/settings.json

    Final JSON must be valid (no trailing commas).
  </action>
  <verify>
    <automated>node -e "const s=require('C:/Users/marcu/AppData/Roaming/Code/User/settings.json'); console.log(s['editor.fontFamily'])"</automated>
  </verify>
  <done>"editor.fontFamily" key exists in settings.json with Calibri as the primary font. VS Code picks up the change immediately on next window focus (no restart needed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Added editor.fontFamily = Calibri to VS Code user settings (global, applies to all themes including Prism).</what-built>
  <how-to-verify>
    1. Switch focus to VS Code (or open it).
    2. Open any code file.
    3. Confirm the editor text renders in Calibri — it is a proportional sans-serif, noticeably different from monospace fonts like Consolas or Courier.
    4. Switch between your two Prism theme variants and confirm Calibri is active in both.
    5. (Optional) On a Mac: open VS Code, check File > Preferences > Settings, search "fontFamily" and confirm the value shows Calibri.
  </how-to-verify>
  <resume-signal>Type "approved" if Calibri is rendering correctly, or describe what you see if it is not.</resume-signal>
</task>

</tasks>

<verification>
- settings.json is valid JSON: `node -e "require('C:/Users/marcu/AppData/Roaming/Code/User/settings.json')"`
- editor.fontFamily key is present and starts with "Calibri"
- All pre-existing keys in settings.json are unchanged
</verification>

<success_criteria>
VS Code editor font is Calibri on Windows. The cross-platform font stack ensures graceful fallback on Mac. The setting is theme-agnostic and therefore applies to both Prism theme variants (and all other themes).
</success_criteria>

<output>
No SUMMARY.md needed for quick tasks. Return "approved" to complete.
</output>
